// JQuery provided by Materialize
$(document).ready(function() {
//  takes user input and submits the form
    $('select').formSelect();
// open the mobile navbar on the right
    $('.sidenav').sidenav({edge:'right'});
// allows the collapsible menus to open
    $('.collapsible').collapsible();
// allows for the use of a date picker
    $('.datepicker').datepicker();
// allows for the use of a time picker
    $('.timepicker').timepicker();
// allows for the tabs for my map section to work as intented
    $('.tabs').tabs();
  });
;